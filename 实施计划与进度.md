# Coding Agent：实施计划与进度

> 更新规则：每完成一个可验收事项，勾选对应条目，并在“进度记录”追加日期、结果和验证方式。未达到验收条件，不标记完成。

## 项目目标

交付一个可在指定 workspace 中自主完成小型修复任务的 Coding Agent。模型只能提出工具请求；文件访问、命令执行和高风险操作均由确定性的安全策略决定是否执行。

## 范围边界

### 本期包含

- 命令行入口、真实 LLM 调用和多轮 Tool Calling；
- 受 workspace 约束的文件读取与修改；
- 受策略控制的本地命令执行与人工审批；
- 路径越界、符号链接逃逸和敏感文件保护；
- 审计日志、变更摘要及安全测试；
- 一个包含 Prompt Injection 的端到端演示。

### 本期不包含

- Multi-Agent、MCP、长期记忆、GUI 或 IDE 插件；
- 云端执行、自动联网搜索、多仓库协作；
- 完整 Shell 语义分析或容器级隔离；
- 无法可靠保证不影响用户既有修改的自动回滚。

## 阶段计划

### P0：项目骨架与配置

**目标：** 建立可运行、可测试且不会提交凭据的 Python 项目骨架。

**范围：** `pyproject.toml`、包目录、CLI 配置加载、`.gitignore`、测试基础设施。

**不包含：** Agent 循环、工具实现和策略判断。

**验收条件：**

- [x] 可通过单一命令启动 CLI 并显示帮助信息；
- [x] workspace、模型和执行限制可由配置或环境变量提供；
- [x] API Key 仅从环境变量或本地未提交配置读取；
- [x] 测试命令可执行，且 `.gitignore` 忽略本地凭据与运行日志。

**状态：** 完成（2026-08-27）

### P1：最小 Agent 闭环

**目标：** Agent 能完成“读取项目 → 运行测试 → 修改文件 → 再测 → 结束”的基本循环。

**范围：** LLM Client、消息与上下文、Tool Registry、`list_files`、`read_file`、`apply_patch`、`run_command`、Agent Runtime。

**不包含：** 高风险审批、完整安全策略、回滚。

**验收条件：**

- [x] LLM 返回 Tool Call 时，运行时可校验、执行并将结果作为观察结果回传；
- [x] 可配置 `max_steps`、单命令超时和工具输出上限；
- [x] 可恢复的文件或命令失败会返回给模型，而非直接崩溃；
- [x] 连续重复的失败工具请求会受限；
- [x] 在普通示例项目中，Agent 能修复一个失败测试并得到通过结果。

**状态：** 完成（2026-08-28）

### P2：文件与命令安全边界

**目标：** 所有工具调用均先经过确定性策略判断，模型无法绕过 workspace 或执行明显危险操作。

**范围：** Workspace Manager、真实路径校验、Path Guard、Sensitive Data Guard、Command Policy、风险分级与审批接口。

**不包含：** 通用 Shell 静态分析；网络工具；容器隔离。

**验收条件：**

- [x] 所有文件路径在规范化并解析符号链接后，仍必须位于 workspace 内；
- [x] 路径穿越、绝对路径越界和符号链接逃逸均被拒绝；
- [x] `.env`、私钥和已定义的凭据路径均被拒绝读取或写入；
- [x] 策略对每次调用输出 `ALLOW`、`REQUIRE_APPROVAL` 或 `DENY`，并附风险等级与原因；
- [x] `sudo`、关机、格式化等关键危险命令被直接拒绝；
- [x] 删除、依赖安装、破坏性 Git 操作等 HIGH 风险命令需要用户明确批准；
- [x] 即使命令获准，也在 workspace cwd、超时和输出上限内执行。

**状态：** 完成（2026-08-29）

### P3：可观测性与变更控制

**目标：** 每次运行都能说明执行了什么、为什么允许或拒绝，以及改动了哪些文件。

**范围：** 结构化审计日志、Git 状态基线、diff 摘要、任务结束摘要、可选 checkpoint。

**不包含：** 自动恢复用户在任务开始前已存在的未提交改动。

**验收条件：**

- [x] `.agent/logs/` 中记录任务、步骤、工具、策略决定、风险、耗时和退出状态；
- [x] 文件修改记录路径、变更类型和 diff 摘要；
- [x] 任务结束输出修改文件数、测试结果、被拦截操作和审批次数；
- [x] 启动时记录已有 Git 状态，摘要不会把其误报为本次 Agent 改动；
- [x] 未实现自动 rollback；不承诺恢复任务前已有的用户修改。

**状态：** 完成（2026-08-30）

### P4：安全测试与演示

**目标：** 用自动化测试和端到端演示证明安全机制不会阻断正常修复任务。

**范围：** 单元测试、集成测试、安全测试、恶意仓库样例、演示脚本与 README。

**不包含：** 以“模型必然服从恶意文本”为测试前提。

**验收条件：**

- [x] 安全测试覆盖路径穿越、绝对路径、符号链接、敏感文件、超时、超大输出和危险命令；
- [x] 测试覆盖无效参数、重复失败调用和达到 `max_steps` 的终止行为；
- [x] 恶意 README 中的越权指令无法导致实际敏感文件访问；
- [x] 演示任务可在同一运行中拦截越权请求、修复代码并通过测试；
- [x] README 说明安装、配置、运行、安全模型、限制和测试方式；
- [x] 演示材料能在两分钟内展示核心闭环与安全边界。

**状态：** 完成（2026-08-30）

### P5：真实环境验收与交付准备

**目标：** 在隔离的临时 workspace 中完成一次真实 LLM 修复任务，并确认交付物可安装、可追踪且不包含凭据。

**范围：** 通用 OpenAI-compatible Responses 端点配置、客户端本地续轮与端点内部适配、真实 API 冒烟测试、审计结果人工核对、安装验证和用户执行的最终 Git 提交。

**不包含：** 新功能开发、自动发布、远程推送或版本标签。

**验收条件：**

- [x] 本地未提交配置文件可配置任意 OpenAI-compatible Responses 端点，且 API Key 不会输出；
- [x] 所有兼容端点均使用客户端维护的无状态续轮上下文，不依赖服务端会话记录；
- [x] LLM 层保留所有模型输出项并解析工具调用与用量；服务商仅由本地配置文件选择，不作为 CLI 参数出现；
- [ ] 使用用户提供的 API 环境，在临时 workspace 中通过 CLI 完成一次“运行测试 → 修改 → 再测”的真实模型任务；
- [ ] 人工检查该任务的 CLI 摘要与 `.agent/logs/`：改动、测试结果、风险决策和已有 Git 状态均可追踪，且不含凭据内容；
- [ ] 在干净 Python 环境中完成安装、`coding-agent --help` 和完整测试套件验证；
- [ ] 用户复核暂存内容后自行创建最终 Git 提交，确认没有凭据、临时文件或运行日志被纳入。

**状态：** 进行中（通用兼容层完成；需要用户的 API 环境）

### P6：持续会话与本地可视化界面

**目标：** 让同一 Agent 会话持续处理多条用户消息，在进程重启后安全恢复已完成的本地上下文，并通过本地网页观察执行、审批和结果。

**范围：** 客户端本地 history 追加、SQLite 会话持久化、会话队列与 workspace 互斥、终端 REPL、FastAPI 本地服务、React + Vite 工作台、SSE 事件、网页审批、取消控制、会话删除、会话级审计与上下文限制。

**不包含：** 自动重放服务重启时正在执行的 turn、服务端 LLM 会话、流式 token、远程 Web 暴露、认证系统、自动 history 总结或长期记忆。

**验收条件：**

- [x] 同一 session 的后续用户消息追加到本地 history；新 session 不共享 history；
- [x] `coding-agent chat` 提供 `/new`、`/sessions`、`/open`、`/status`、`/cancel`、`/quit` 交互；
- [x] 每个 workspace 将 session 元数据、客户端 transcript 和安全事件存入 `.agent/conversations/sessions.sqlite3`；不存 API Key，Agent 工具无法读取该目录；
- [x] 服务重启后恢复已完成会话；原本运行中的 turn 标记为中断并等待下一条明确用户消息，绝不自动重放；
- [x] `coding-agent serve` 提供仅 loopback 的会话页面、REST 消息入口和 SSE 事件流；
- [x] 页面仅接收安全事件摘要，不包含 API Key、原始 reasoning 或补丁/文件正文；
- [x] 网页审批只作用于对应调用，拒绝、超时与取消默认拒绝；
- [x] 同一 workspace 的 Agent turn 串行执行；会话达到 turn/history 限制时要求新建会话；
- [x] 取消可阻止后续模型/工具调用，并终止运行中的本地命令；
- [x] 自动测试覆盖 history 追加、会话串行、取消命令、持久化恢复与删除、运行中断保护、网页 API、网页审批、敏感目录拒绝和文本回退解析；
- [x] React + Vite 工作台提供会话列表、事件时间线、可折叠工具详情、运行摘要、取消、审批和确认后删除会话；
- [ ] 使用真实 DeepSeek 在浏览器完成至少两条连续用户消息与一次受控工具调用的手工验收。

**状态：** 进行中（实现与自动化验证完成；真实浏览器验收待执行）

## 总体验收清单

- [ ] CLI 可指定 workspace 并启动真实 LLM；
- [ ] Agent 可多轮调用读取、修改和命令工具；
- [ ] 至少一个真实修复任务完整通过；
- [ ] workspace 外和敏感文件访问被拒绝；
- [ ] 高风险操作可审批，关键危险操作被拒绝；
- [ ] 步数、时间和输出受限；
- [ ] 审计日志和变更摘要可追踪；
- [ ] 安全测试与 Prompt Injection 演示通过；
- [ ] 仓库不包含 API Key 或其他真实凭据。

## 进度记录

| 日期 | 阶段 | 状态 | 完成内容 | 验证方式 | 后续动作 |
| --- | --- | --- | --- | --- | --- |
| 2026-08-27 | 计划建立 | 完成 | 根据《项目启动文档》整理实施范围、阶段与验收标准 | 人工核对计划与原始设计 | 从 P0 开始实施 |
| 2026-08-27 | P0 | 完成 | 建立 Python 包、CLI、配置加载、忽略规则与基础测试；新增旧版构建环境兼容配置 | `coding-agent --help`、`python -m unittest discover -s tests -t .`、配置脱敏输出与忽略规则检查均通过 | 开始 P1：最小 Agent 闭环 |
| 2026-08-28 | P1 | 完成 | 实现 Responses API 适配器、Tool Registry、文件/补丁/命令工具、受限 Agent 循环及任务 CLI | `python -m unittest discover -s tests -t .`：11 项通过；包含失败测试修复闭环和重复失败熔断 | 开始 P2：安全边界 |
| 2026-08-29 | P2 | 完成 | 实现确定性策略、真实路径保护、敏感数据拦截、命令风险分类和交互式审批 | `python -m unittest discover -s tests -t .`：18 项通过；覆盖越界、符号链接、敏感路径、危险命令和审批 | 开始 P3：可观测性与变更控制 |
| 2026-08-30 | P3 | 完成 | 实现 JSONL 审计、补丁 diff 摘要、任务结束汇总和 Git 只读基线；不提供自动回滚 | `python -m unittest discover -s tests -t .`：22 项通过；基线测试注入假 runner，验证过程未执行 Git | 开始 P4：安全测试与演示 |
| 2026-08-30 | P4 | 完成 | 补齐超时、输出上限、无效参数和步数限制测试；新增恶意 README 离线演示 | `python -m unittest discover -s tests -t .`：27 项通过；演示在同次运行中拦截 `.env` 读取、修复 `app.py` 并通过测试 | 进行最终人工验收与真实 API 冒烟测试（需用户提供环境） |
| 2026-08-30 | P5 | 进行中 | 完成无厂商预设的通用 Responses 配置和统一的本地无状态续轮 | `python -m unittest discover -s tests -t .`：31 项通过；验证仅通过通用 API Key、base URL 与模型名配置端点，且请求不含 `previous_response_id` | 使用真实兼容端点完成最终冒烟测试 |
| 2026-08-31 | P5 | 进行中 | 重构 LLM 层为通用 Responses 客户端与内部端点适配器；客户端重放完整输出项和工具结果，新增用量解析及生成 token 上限 | `python -m unittest discover -s tests -t .`：33 项通过；覆盖本地历史重放、OpenAI 专属参数、DeepSeek 省略不兼容参数、通用端点请求及无服务名的内部选择 | 使用真实兼容端点完成最终冒烟测试 |
| 2026-08-31 | P5 | 进行中 | 支持从本地未提交 `.env` 文件读取 API Key、base URL 与模型名；新增隔离的 LLM 请求/响应观察脚本 | `PYTHONPATH=src python debug/observe_llm.py --help` 可用；`python -m unittest discover -s tests -t .`：36 项通过，覆盖配置文件读取、优先级和 CLI 脱敏输出 | 使用真实兼容端点完成最终冒烟测试 |
| 2026-08-31 | P5 | 进行中 | 改为由 `.env` 中的显式 provider 选择 OpenAI、DeepSeek 或通用 Responses 适配器；主 CLI 移除全部 LLM 配置入口 | `python -m unittest discover -s tests -t .`：38 项通过；`PYTHONPATH=src python -m agent --help` 确认不含 provider、API、model、base URL 或 token 配置参数 | 使用真实兼容端点完成最终冒烟测试 |
| 2026-08-31 | P6 | 进行中 | 实现本地持续会话、终端 chat、loopback Web 页面、SSE 事件、网页审批、会话级审计、上下文上限和命令取消 | `python -m unittest discover -s tests -t .`：46 项通过；覆盖多用户消息 history、会话队列、取消命令、网页配置脱敏、网页会话 API、网页审批和文本回退解析 | 使用真实 DeepSeek 在浏览器完成连续对话与工具调用验收 |
| 2026-08-31 | P6 | 进行中 | 将原生单页替换为 React + TypeScript + Vite 工作台；前端源码位于仓库根目录 `frontend/`，FastAPI 同源提供构建资源，后端会话与 SSE 接口保持不变 | 前端生产构建通过；增加静态 bundle 服务测试 | 使用真实 DeepSeek 在浏览器完成连续对话与工具调用验收 |
| 2026-08-31 | P6 | 进行中 | 增加 workspace 本地 SQLite 会话存储、transcript 恢复、运行中断保护、终端会话选择、Web 会话删除和 `.agent` 工具隔离 | `python -m unittest discover -s tests -t .`：51 项通过；`npm run build` 通过；wheel 包含新存储模块和当前静态资源 | 使用真实 DeepSeek 在浏览器完成连续对话与工具调用验收 |
| 2026-08-31 | P6 | 进行中 | 重构 React 工作台视觉：突出对话区、收起式运行详情、轻量会话列表、悬浮输入区和紧凑工具状态卡；后端交互保持不变 | `python -m unittest discover -s tests -t .`：51 项通过；`npm run build` 通过并生成当前静态资源 | 在真实浏览器确认视觉与交互体验 |
| 2026-08-31 | P6 | 进行中 | 改为中性深灰极简配色；桌面端固定为视口布局，主时间线独立滚动，输入区固定在聊天区底部 | `python -m unittest discover -s tests -t .`：51 项通过；`npm run build` 通过并生成当前静态资源 | 在真实浏览器确认视觉与长对话滚动体验 |
| 2026-08-31 | P6 | 进行中 | 调整为白底极简界面；以 flex 固定聊天高度，时间线独立滚动，输入栏作为不可收缩的底部区域 | `python -m unittest discover -s tests -t .`：51 项通过；`npm run build` 通过并生成当前静态资源 | 在真实浏览器确认输入栏始终可见与长对话滚动体验 |
| 2026-08-31 | P6 | 进行中 | 输入栏改为聊天式自适应 textarea：禁止手动拉伸、随内容增高、达到上限后内部滚动，发送按钮嵌入输入容器 | `python -m unittest discover -s tests -t .`：51 项通过；`npm run build` 通过并生成当前静态资源 | 在真实浏览器确认多行输入与发送交互体验 |

## 更新模板

```text
日期：YYYY-MM-DD
阶段：P0 / P1 / P2 / P3 / P4
状态：进行中 / 完成 / 受阻
完成内容：
验证方式：
遗留风险或阻塞：
下一步：
```
