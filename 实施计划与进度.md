# Coding Agent：实施计划与进度

> 更新规则：每完成一个可验收事项，勾选对应条目，并在“进度记录”追加日期、结果和验证方式。未达到验收条件，不标记完成。

## 项目目标

交付一个可在指定 workspace 中自主完成小型修复任务的 Coding Agent。模型只能提出工具请求；文件访问、命令执行和高风险操作均由确定性的安全策略决定是否执行。

## 范围边界

### 本期包含

- 命令行入口、真实 LLM 调用和多轮 Tool Calling；
- 受 workspace 约束的文件读取与修改；
- 受策略控制的本地命令执行与人工审批；
- 路径越界、符号链接逃逸和敏感文件保护；
- 审计日志、变更摘要及安全测试；
- Prompt Injection 边界的自动化安全测试。

### 本期不包含

- Multi-Agent、MCP、长期记忆、GUI 或 IDE 插件；
- 云端执行、自动联网搜索、多仓库协作；
- 完整 Shell 语义分析、临时工作区、资源配额或细粒度权限档位；
- 无法可靠保证不影响用户既有修改的自动回滚。

## 阶段计划

### P0：项目骨架与配置

**目标：** 建立可运行、可测试且不会提交凭据的 Python 项目骨架。

**范围：** `pyproject.toml`、包目录、CLI 配置加载、`.gitignore`、测试基础设施。

**不包含：** Agent 循环、工具实现和策略判断。

**验收条件：**

- [x] 可通过单一命令启动 CLI 并显示帮助信息；
- [x] workspace、模型和执行限制可由配置或环境变量提供；
- [x] API Key 仅从环境变量或本地未提交配置读取；
- [x] 测试命令可执行，且 `.gitignore` 忽略本地凭据与运行日志。

**状态：** 完成（2026-08-27）

### P1：最小 Agent 闭环

**目标：** Agent 能完成“读取项目 → 运行测试 → 修改文件 → 再测 → 结束”的基本循环。

**范围：** LLM Client、消息与上下文、Tool Registry、`list_files`、`read_file`、`apply_patch`、`run_command`、Agent Runtime。

**不包含：** 高风险审批、完整安全策略、回滚。

**验收条件：**

- [x] LLM 返回 Tool Call 时，运行时可校验、执行并将结果作为观察结果回传；
- [x] 可配置 `max_steps`、单命令超时和工具输出上限；
- [x] 可恢复的文件或命令失败会返回给模型，而非直接崩溃；
- [x] 连续重复的失败工具请求会受限；
- [x] 在普通示例项目中，Agent 能修复一个失败测试并得到通过结果。

**状态：** 完成（2026-08-28）

### P2：文件与命令安全边界

**目标：** 所有工具调用均先经过确定性策略判断，模型无法绕过 workspace 或执行明显危险操作。

**范围：** Workspace Manager、真实路径校验、Path Guard、Sensitive Data Guard、Command Policy、风险分级与审批接口。

**不包含：** 通用 Shell 静态分析；网络工具；容器隔离。

**验收条件：**

- [x] 所有文件路径在规范化并解析符号链接后，仍必须位于 workspace 内；
- [x] 路径穿越、绝对路径越界和符号链接逃逸均被拒绝；
- [x] `.env`、私钥和已定义的凭据路径均被拒绝读取或写入；
- [x] 策略对每次调用输出 `ALLOW`、`REQUIRE_APPROVAL` 或 `DENY`，并附风险等级与原因；
- [x] `sudo`、关机、格式化等关键危险命令被直接拒绝；
- [x] 删除、依赖安装、破坏性 Git 操作等 HIGH 风险命令需要用户明确批准；
- [x] 即使命令获准，也在 workspace cwd、超时和输出上限内执行。

**状态：** 完成（2026-08-29）

### P3：可观测性与变更控制

**目标：** 每次运行都能说明执行了什么、为什么允许或拒绝，以及改动了哪些文件。

**范围：** 结构化审计日志、Git 状态基线、diff 摘要、任务结束摘要、可选 checkpoint。

**不包含：** 自动恢复用户在任务开始前已存在的未提交改动。

**验收条件：**

- [x] `.agent/logs/` 中记录任务、步骤、工具、策略决定、风险、耗时和退出状态；
- [x] 文件修改记录路径、变更类型和 diff 摘要；
- [x] 任务结束输出修改文件数、测试结果、被拦截操作和审批次数；
- [x] 启动时记录已有 Git 状态，摘要不会把其误报为本次 Agent 改动；
- [x] 未实现自动 rollback；不承诺恢复任务前已有的用户修改。

**状态：** 完成（2026-08-30）

### P4：安全测试与文档

**目标：** 用自动化测试证明安全机制不会阻断正常修复任务。

**范围：** 单元测试、集成测试、安全测试与 README。

**不包含：** 以“模型必然服从恶意文本”为测试前提。

**验收条件：**

- [x] 安全测试覆盖路径穿越、绝对路径、符号链接、敏感文件、超时、超大输出和危险命令；
- [x] 测试覆盖无效参数、重复失败调用和达到 `max_steps` 的终止行为；
- [x] 不可信 README 中的越权指令无法导致实际敏感文件访问；
- [x] 同一自动化测试中可拦截越权请求、修复代码并通过测试；
- [x] README 说明安装、配置、运行、安全模型、限制和测试方式；

**状态：** 完成（2026-08-30）

### P5：真实环境验收与交付准备

**目标：** 在隔离的临时 workspace 中完成一次真实 LLM 修复任务，并确认交付物可安装、可追踪且不包含凭据。

**范围：** 通用 OpenAI-compatible Responses 端点配置、客户端本地续轮与端点内部适配、真实 API 冒烟测试、审计结果人工核对、安装验证和用户执行的最终 Git 提交。

**不包含：** 新功能开发、自动发布、远程推送或版本标签。

**验收条件：**

- [x] 本地未提交配置文件可配置任意 OpenAI-compatible Responses 端点，且 API Key 不会输出；
- [x] 所有兼容端点均使用客户端维护的无状态续轮上下文，不依赖服务端会话记录；
- [x] LLM 层保留所有模型输出项并解析工具调用与用量；服务商仅由本地配置文件选择，不作为 CLI 参数出现；
- [ ] 使用用户提供的 API 环境，在临时 workspace 中通过 CLI 完成一次“运行测试 → 修改 → 再测”的真实模型任务；
- [ ] 人工检查该任务的 CLI 摘要与 `.agent/logs/`：改动、测试结果、风险决策和已有 Git 状态均可追踪，且不含凭据内容；
- [ ] 在干净 Python 环境中完成安装、`coding-agent --help` 和完整测试套件验证；
- [ ] 用户复核暂存内容后自行创建最终 Git 提交，确认没有凭据、临时文件或运行日志被纳入。

**状态：** 进行中（通用兼容层完成；需要用户的 API 环境）

### P6：持续会话与本地可视化界面

**目标：** 让同一 Agent 会话持续处理多条用户消息，在进程重启后安全恢复已完成的本地上下文，并通过本地网页观察执行、审批和结果。

**范围：** 客户端本地 history 追加、SQLite 会话持久化、会话队列与 workspace 互斥、终端 REPL、FastAPI 本地服务、React + Vite 工作台、SSE 事件、网页审批、取消控制、会话删除、会话级审计与上下文限制。

**不包含：** 自动重放服务重启时正在执行的 turn、服务端 LLM 会话、流式 token、远程 Web 暴露、认证系统、自动 history 总结或长期记忆。

**验收条件：**

- [x] 同一 session 的后续用户消息追加到本地 history；新 session 不共享 history；
- [x] `coding-agent chat` 提供 `/new`、`/sessions`、`/open`、`/status`、`/cancel`、`/quit` 交互；
- [x] 每个 workspace 将 session 元数据、客户端 transcript 和安全事件存入 `.agent/conversations/sessions.sqlite3`；不存 API Key，Agent 工具无法读取该目录；
- [x] 服务重启后恢复已完成会话；原本运行中的 turn 标记为中断并等待下一条明确用户消息，绝不自动重放；
- [x] `coding-agent serve` 提供仅 loopback 的会话页面、REST 消息入口和 SSE 事件流；
- [x] 页面仅接收安全事件摘要，不包含 API Key、原始 reasoning 或补丁/文件正文；
- [x] 网页审批只作用于对应调用，拒绝、超时与取消默认拒绝；
- [x] 同一 workspace 的 Agent turn 串行执行；会话可持续接收新消息，完整历史不会因上下文管理而静默删除；
- [x] 取消可阻止后续模型/工具调用，并终止运行中的本地命令；
- [x] 自动测试覆盖 history 追加、会话串行、取消命令、持久化恢复与删除、运行中断保护、网页 API、网页审批、敏感目录拒绝和文本回退解析；
- [x] React + Vite 工作台提供会话列表、事件时间线、可折叠工具详情、运行摘要、取消、审批和确认后删除会话；
- [ ] 使用真实 DeepSeek 在浏览器完成至少两条连续用户消息与一次受控工具调用的手工验收。

**状态：** 完成（真实模型与浏览器验收已由用户确认）

### P7：结构化运行追踪

**目标：** 用按轮次关联的模型/工具追踪替代扁平执行事件，并允许用户在本地按需查看脱敏后的模型请求与响应。

**范围：** 私有本地 trace 持久化、模型调用与工具执行的父子关系、请求/响应按需接口、凭据脱敏与内容限额、追踪树与详情面板。

**不包含：** 模型原始推理记录、认证信息记录、远程上传日志、自动代码审查和工作区回滚。

**验收条件：**

- [x] 每轮追踪模型调用、工具执行、时长、结果与策略决策，普通 SSE 不携带请求/响应正文；
- [x] 前端按轮次展示模型、工具、安全和错误筛选，具体节点才请求详情；
- [x] 请求与响应中的常见密钥被脱敏，超长文本明确标记截断，回放用加密 reasoning 不写入追踪；
- [x] 自动测试覆盖追踪结构、正文按需读取、脱敏和通用 LLM 适配器回调。

**状态：** 进行中（自动化验证完成；待真实浏览器确认）

### P8：本地上下文预算与长会话管理

**目标：** 在保留完整本地转录的前提下，为每次模型请求构造有预算、可解释、可恢复的工作上下文。

**范围：** 固定 DeepSeek-V4-Flash 能力基线、保守 token 估算与用量校准、增量结构化摘要、长工具输出工件化及分段读取、SQLite 上下文状态恢复、上下文决策追踪与 Web 展示。

**不包含：** 跨会话记忆、用户画像、向量检索、服务端会话、原始推理保存。

**验收条件：**

- [x] 完整 provider transcript 与模型请求视图分离；压缩不会改写或丢弃原始历史；
- [x] 请求上下文使用固定 1M 窗口、384K 输出保留和安全余量，不新增环境变量覆盖；
- [x] 完成的旧轮次可增量汇总为可校验的本地结构化摘要；摘要失败时保留原文并记录错误；
- [x] 长工具输出保留在本地转录中，后续请求使用摘要/引用，Agent 可通过只读工具分段读取；
- [x] 运行追踪和右侧状态展示上下文规模、摘要版本与工件数量；
- [x] 自动测试覆盖摘要边界、原始历史恢复、工件读取、token 校准和上下文超限重试；
- [ ] 使用真实浏览器完成一次长会话，确认摘要条目、上下文详情和工件读取体验。

**状态：** 进行中（自动化验证完成；待真实浏览器确认）

### P9：默认本地命令沙箱

**目标：** 让每次 `run_command` 都在默认隔离环境中执行，降低项目脚本对宿主机文件、网络和凭据的影响范围。

**范围：** Bubblewrap 可用性预检、Linux 用户/进程/IPC/UTS/网络命名空间、只读运行时挂载、隔离的 HOME 与临时目录、最小环境变量、敏感工作区路径遮蔽、沙箱事实追踪与失败关闭。

**不包含：** 权限档位、宿主机执行授权、临时 OverlayFS 工作区、CPU/内存配额、网络例外或依赖安装策略。

**验收条件：**

- [x] `run_command` 默认经 Bubblewrap 启动，网络默认关闭，且不继承 Agent API 配置或宿主机 HOME；
- [x] 工作区外文件、`.env`、`credentials/` 等已定义敏感路径在沙箱命令内不可读取；
- [x] 沙箱仍可在真实工作区运行项目命令，并保留超时、取消、输出限制与进程组终止；
- [x] Bubblewrap 或必需命名空间不可用时，命令明确失败，不静默退回宿主机执行；
- [x] 审计与运行追踪可显示沙箱、网络、工作区访问和敏感路径遮蔽事实；
- [x] 自动测试覆盖失败关闭、环境/路径/网络隔离及既有命令边界。

**状态：** 完成（2026-09-02；自动化与本机 Bubblewrap 验证通过）

### P10：中文系统提示词与任务执行规范

**目标：** 用中文、可验证的系统提示词统一 Agent 的任务判断、代码修改、工具使用与最终汇报行为。

**范围：** 主 Agent 提示词的中文化与执行闭环、代码可读性和 Git 边界、工具/沙箱/不可信输入规则、最终回复证据要求；本地上下文摘要提示词的中文化及 JSON 字段契约测试。

**不包含：** 工具 Schema、工具错误文本、权限档位、前端文案、模型厂商配置或用户可编辑提示词。

**验收条件：**

- [x] 每个 Agent 请求使用中文主提示词，明确当前用户目标、分析只读、最小修改和相关验证的工作方式；
- [x] 提示词要求默认中文回复、必要注释、保持项目风格，并禁止无授权 Git 写入或覆盖既有改动；
- [x] 提示词明确工具 Schema、沙箱、策略拒绝和不可信数据边界，且不把提示词当作安全执行机制；
- [x] 最终回复要求基于工具证据汇报完成内容、验证结果与阻塞；
- [x] 上下文摘要提示词改为中文，仍只允许既有英文 JSON 字段；
- [x] 自动测试覆盖主提示词要点、运行时传递和摘要字段契约。

**状态：** 完成（2026-09-02；自动化验证通过）

### P11：移除会话轮次硬上限

**目标：** 让本地会话可持续接收用户消息，由上下文预算控制单次模型请求规模，而非以总轮次终止会话。

**范围：** 删除运行时配置、会话拒绝逻辑、终端与网页计数展示；保留每轮内部追踪编号、单轮工具步数、任务超时与上下文预算；兼容旧 SQLite 的遗留字段和已达上限状态。

**不包含：** 修改单轮 `max_steps`、任务超时、上下文预算、转录保留策略或删除会话机制。

**验收条件：**

- [x] 新消息不会因为会话已处理轮数而被拒绝；关闭会话仍会拒绝新消息；
- [x] CLI、REST 快照和 React 页面不再暴露会话轮次上限或 `已处理轮次` 计数；
- [x] 旧数据库中的 `max_turns` 仅作为兼容字段保留，不参与运行判断；旧 `limit_reached` 会话升级后可继续；
- [x] 自动测试覆盖连续消息、旧数据库恢复与前端生产构建。

**状态：** 完成（2026-09-02；自动化验证通过）

## 总体验收清单

- [ ] CLI 可指定 workspace 并启动真实 LLM；
- [ ] Agent 可多轮调用读取、修改和命令工具；
- [ ] 至少一个真实修复任务完整通过；
- [ ] workspace 外和敏感文件访问被拒绝；
- [ ] 高风险操作可审批，关键危险操作被拒绝；
- [ ] 步数、时间和输出受限；
- [ ] 审计日志和变更摘要可追踪；
- [ ] 安全测试与 Prompt Injection 边界验证通过；
- [ ] 仓库不包含 API Key 或其他真实凭据。

## 进度记录

| 日期 | 阶段 | 状态 | 完成内容 | 验证方式 | 后续动作 |
| --- | --- | --- | --- | --- | --- |
| 2026-08-27 | 计划建立 | 完成 | 根据《项目启动文档》整理实施范围、阶段与验收标准 | 人工核对计划与原始设计 | 从 P0 开始实施 |
| 2026-08-27 | P0 | 完成 | 建立 Python 包、CLI、配置加载、忽略规则与基础测试；新增旧版构建环境兼容配置 | `coding-agent --help`、`python -m unittest discover -s tests -t .`、配置脱敏输出与忽略规则检查均通过 | 开始 P1：最小 Agent 闭环 |
| 2026-08-28 | P1 | 完成 | 实现 Responses API 适配器、Tool Registry、文件/补丁/命令工具、受限 Agent 循环及任务 CLI | `python -m unittest discover -s tests -t .`：11 项通过；包含失败测试修复闭环和重复失败熔断 | 开始 P2：安全边界 |
| 2026-08-29 | P2 | 完成 | 实现确定性策略、真实路径保护、敏感数据拦截、命令风险分类和交互式审批 | `python -m unittest discover -s tests -t .`：18 项通过；覆盖越界、符号链接、敏感路径、危险命令和审批 | 开始 P3：可观测性与变更控制 |
| 2026-08-30 | P3 | 完成 | 实现 JSONL 审计、补丁 diff 摘要、任务结束汇总和 Git 只读基线；不提供自动回滚 | `python -m unittest discover -s tests -t .`：22 项通过；基线测试注入假 runner，验证过程未执行 Git | 开始 P4：安全测试与演示 |
| 2026-08-30 | P4 | 完成 | 补齐超时、输出上限、无效参数和步数限制测试；新增不可信 README 安全回归 | `python -m unittest discover -s tests -t .`：27 项通过；测试在同次运行中拦截 `.env` 读取、修复 `app.py` 并通过测试 | 进行最终人工验收与真实 API 冒烟测试（需用户提供环境） |
| 2026-08-30 | P5 | 进行中 | 完成无厂商预设的通用 Responses 配置和统一的本地无状态续轮 | `python -m unittest discover -s tests -t .`：31 项通过；验证仅通过通用 API Key、base URL 与模型名配置端点，且请求不含 `previous_response_id` | 使用真实兼容端点完成最终冒烟测试 |
| 2026-08-31 | P5 | 进行中 | 重构 LLM 层为通用 Responses 客户端与内部端点适配器；客户端重放完整输出项和工具结果，新增用量解析及生成 token 上限 | `python -m unittest discover -s tests -t .`：33 项通过；覆盖本地历史重放、OpenAI 专属参数、DeepSeek 省略不兼容参数、通用端点请求及无服务名的内部选择 | 使用真实兼容端点完成最终冒烟测试 |
| 2026-08-31 | P5 | 进行中 | 支持从本地未提交 `.env` 文件读取 API Key、base URL 与模型名；新增隔离的 LLM 请求/响应观察脚本 | `PYTHONPATH=src python debug/observe_llm.py --help` 可用；`python -m unittest discover -s tests -t .`：36 项通过，覆盖配置文件读取、优先级和 CLI 脱敏输出 | 使用真实兼容端点完成最终冒烟测试 |
| 2026-08-31 | P5 | 进行中 | 改为由 `.env` 中的显式 provider 选择 OpenAI、DeepSeek 或通用 Responses 适配器；主 CLI 移除全部 LLM 配置入口 | `python -m unittest discover -s tests -t .`：38 项通过；`PYTHONPATH=src python -m agent --help` 确认不含 provider、API、model、base URL 或 token 配置参数 | 使用真实兼容端点完成最终冒烟测试 |
| 2026-08-31 | P6 | 进行中 | 实现本地持续会话、终端 chat、loopback Web 页面、SSE 事件、网页审批、会话级审计、上下文上限和命令取消 | `python -m unittest discover -s tests -t .`：46 项通过；覆盖多用户消息 history、会话队列、取消命令、网页配置脱敏、网页会话 API、网页审批和文本回退解析 | 使用真实 DeepSeek 在浏览器完成连续对话与工具调用验收 |
| 2026-08-31 | P6 | 进行中 | 将原生单页替换为 React + TypeScript + Vite 工作台；前端源码位于仓库根目录 `frontend/`，FastAPI 同源提供构建资源，后端会话与 SSE 接口保持不变 | 前端生产构建通过；增加静态 bundle 服务测试 | 使用真实 DeepSeek 在浏览器完成连续对话与工具调用验收 |
| 2026-08-31 | P6 | 进行中 | 增加 workspace 本地 SQLite 会话存储、transcript 恢复、运行中断保护、终端会话选择、Web 会话删除和 `.agent` 工具隔离 | `python -m unittest discover -s tests -t .`：51 项通过；`npm run build` 通过；wheel 包含新存储模块和当前静态资源 | 使用真实 DeepSeek 在浏览器完成连续对话与工具调用验收 |
| 2026-08-31 | P6 | 进行中 | 重构 React 工作台视觉：突出对话区、收起式运行详情、轻量会话列表、悬浮输入区和紧凑工具状态卡；后端交互保持不变 | `python -m unittest discover -s tests -t .`：51 项通过；`npm run build` 通过并生成当前静态资源 | 在真实浏览器确认视觉与交互体验 |
| 2026-08-31 | P6 | 进行中 | 改为中性深灰极简配色；桌面端固定为视口布局，主时间线独立滚动，输入区固定在聊天区底部 | `python -m unittest discover -s tests -t .`：51 项通过；`npm run build` 通过并生成当前静态资源 | 在真实浏览器确认视觉与长对话滚动体验 |
| 2026-08-31 | P6 | 进行中 | 调整为白底极简界面；以 flex 固定聊天高度，时间线独立滚动，输入栏作为不可收缩的底部区域 | `python -m unittest discover -s tests -t .`：51 项通过；`npm run build` 通过并生成当前静态资源 | 在真实浏览器确认输入栏始终可见与长对话滚动体验 |
| 2026-08-31 | P6 | 进行中 | 输入栏改为聊天式自适应 textarea：禁止手动拉伸、随内容增高、达到上限后内部滚动，发送按钮嵌入输入容器 | `python -m unittest discover -s tests -t .`：51 项通过；`npm run build` 通过并生成当前静态资源 | 在真实浏览器确认多行输入与发送交互体验 |
| 2026-09-01 | P6 | 进行中 | 前端时间线隐藏仅用于审计的 `conversation_created` 事件；会话创建、SSE 与持久化记录保持不变 | `python -m unittest discover -s tests -t .`：51 项通过；`npm run build` 通过并生成当前静态资源 | 在真实浏览器确认新会话空状态 |
| 2026-09-01 | P6 | 进行中 | Agent 回复支持安全的 Markdown/GFM 渲染，包括代码块、列表、引用、表格与链接；不解析模型输出的原始 HTML | `python -m unittest discover -s tests -t .`：51 项通过；`npm run build` 通过并生成当前静态资源 | 在真实浏览器确认 Markdown 展示与代码块滚动体验 |
| 2026-09-01 | P6 | 进行中 | 主对话区仅保留用户与 Agent 消息；运行时以一条原位刷新的轻量状态提示进度，工具和生命周期安全摘要移至右侧可展开的执行记录 | `python -m unittest discover -s tests -t .`：51 项通过；`npm run build` 通过并生成当前静态资源 | 在真实浏览器确认执行状态刷新、右侧详情与长对话滚动体验 |
| 2026-09-01 | P6 | 进行中 | 修正前端遗漏监听的 `user_message` SSE 事件；消息卡按内容收缩，短消息不再占用整块最大宽度 | `npm run build` 通过并生成当前静态资源；按本次要求未运行测试套件 | 在真实浏览器确认用户消息立即出现与短消息卡片宽度 |
| 2026-09-01 | P6 | 进行中 | 完成聊天体验补强：自动跟随/回到最新、会话草稿隔离、带幂等标识的乐观发送与失败重试、中文输入法保护、后台会话未读提示、移动端抽屉、审批焦点管理与 SSE 断线游标恢复 | `python -m unittest discover -s tests -t .`：53 项通过；`npm run build` 通过并生成当前静态资源 | 在真实浏览器验收长对话滚动、断线重连、移动端抽屉和审批键盘操作 |
| 2026-09-01 | P6 | 进行中 | 右侧执行记录改为紧凑摘要并增加“展开记录”；弹窗显示当前会话完整安全事件，支持逐项展开、独立滚动、Esc 关闭与焦点恢复 | `python -m unittest discover -s tests -t .`：53 项通过；`npm run build` 通过并生成当前静态资源 | 在真实浏览器确认大屏记录查看与键盘关闭交互 |
| 2026-09-01 | P6 | 进行中 | “展开记录”由文字下划线调整为浅灰边框实体按钮，保持弹窗行为不变 | `npm run build` 通过并生成当前静态资源 | 在真实浏览器确认按钮视觉与弹窗入口 |
| 2026-09-01 | P6 | 进行中 | 审批弹窗限制为视口内高度，长参数摘要改为内部滚动并始终保留拒绝/批准按钮；顶部隐藏空闲时的“等待输入”状态徽标 | `npm run build` 通过并生成当前静态资源 | 在真实浏览器确认长命令审批与空闲页头展示 |
| 2026-09-01 | P6 | 进行中 | 执行记录弹窗重构为主从双栏：左侧选择事件，右侧独立显示当前事件的安全详情；窄屏自动切换为上下布局 | `python -m unittest discover -s tests -t .`：53 项通过；`npm run build` 通过并生成当前静态资源 | 在真实浏览器确认长详情滚动、条目选择与移动端布局 |
| 2026-09-01 | P6 | 进行中 | 修正单条短消息被时间线网格纵向拉伸的问题：时间线从顶部排布，消息卡始终按内容高度对齐 | `npm run build` 通过并生成当前静态资源 | 在真实浏览器确认单条、短消息与长对话均保持内容高度 |
| 2026-09-01 | P6 | 进行中 | 浏览器本地记录当前 workspace 的最后活动会话；刷新时优先恢复仍存在的会话，否则安全回退到首个会话 | `npm run build` 通过并生成当前静态资源 | 在真实浏览器确认切换会话、刷新、删除会话后的恢复行为 |
| 2026-09-01 | P6 | 进行中 | 非成功终止的 turn 在主界面持续显示可操作提醒，覆盖步数上限、超时、重复工具失败、模型失败、取消和中断；提醒可打开执行记录，事件标签不再误报“已完成” | `python -m unittest discover -s tests -t .`：53 项通过；`npm run build` 通过并生成当前静态资源 | 在真实浏览器确认各终止状态的提醒文案、刷新恢复和记录入口 |
| 2026-09-01 | P6 | 进行中 | 终止提醒移至输入框正上方，提供关闭按钮；关闭状态按会话、轮次和结果类型在浏览器本地有限保存，新的终止结果仍会提醒 | `python -m unittest discover -s tests -t .`：53 项通过；`npm run build` 通过并生成当前静态资源 | 在真实浏览器确认底部位置、关闭后刷新和下一轮结果的提醒行为 |
| 2026-09-01 | P6 | 进行中 | 修复达到工具步数、超时或取消时遗留未配对 function call 的 history 问题；终止时写入“未执行”观察结果，发送前自动修复旧 transcript，避免继续会话被模型端以 400 拒绝 | `python -m unittest discover -s tests -t .`：54 项通过；新增步数终止与旧 history 修复回归覆盖 | 重启服务后在旧会话继续一次，确认模型请求恢复正常 |
| 2026-09-01 | P6 | 进行中 | 会话删除改为幂等；前端以 tombstone 屏蔽迟到的 SSE 与快照刷新，删除失败时恢复可见状态，避免会话删后重现或误报失败 | `python -m unittest discover -s tests -t .`：54 项通过；覆盖重复 DELETE 返回 204；`npm run build` 通过 | 在真实浏览器确认删除当前/非当前会话、刷新与迟到事件下列表不重现 |
| 2026-09-01 | P6 | 进行中 | 前端仅为当前会话保持一条 SSE；后台会话改为低频元数据轮询，避免多会话耗尽浏览器 HTTP/1.1 连接而使删除、审批和消息请求无限等待 | `npm run build` 通过并生成当前静态资源；`python -m unittest discover -s tests -t .`：54 项通过 | 在真实浏览器创建 6 个以上会话后确认删除、审批与消息请求均可立即完成 |
| 2026-09-01 | P5 | 完成 | 用户以真实模型完成项目级多轮任务和测试，确认通用 Responses 配置、上下文续轮与工具调用闭环可用 | 用户真实服务商验收通过 | 进入后续质量与发布准备 |
| 2026-09-01 | P6 | 完成 | 用户确认持续会话、刷新恢复、多会话场景下的删除、审批与消息提交均正常；单 SSE 连接策略通过真实浏览器验收 | 用户真实浏览器验收通过 | 后续按需补充前端自动化回归测试与使用文档 |
| 2026-09-01 | P7 | 进行中 | 以私有结构化 trace 替代扁平执行记录：记录模型/工具父子关系与时长，按需展示脱敏模型请求响应，不经 SSE 发送正文；`list_files` 空路径安全规范化为工作区根目录 | `python -m unittest discover -s tests -t .`：58 项通过；`npm run build` 通过 | 在真实浏览器确认追踪树、筛选和模型详情 |
| 2026-09-01 | P8 | 进行中 | 新增本地上下文管理：完整 transcript 与请求视图分离；固定 DeepSeek-V4-Flash 1M/384K 基线；增量摘要、长工具输出工件化和分段读取；SQLite 恢复及追踪详情展示上下文决策 | `python -m unittest discover -s tests -t .`：67 项通过；`npm run build` 通过 | 在真实浏览器完成一次长会话，确认摘要、工件读取和上下文详情 |
| 2026-09-02 | P9 | 完成 | `run_command` 改为默认 Bubblewrap 沙箱：关闭网络、清理环境变量、隔离 HOME/TMP、遮蔽工作区敏感路径；沙箱不可用时失败关闭；执行记录新增隔离事实 | `PYTHONPATH=src python -m unittest discover -s tests -t .`：70 项通过；本机 Bubblewrap 验证敏感路径、工作区外文件、API Key 与网络均不可访问 | 后续单独设计并实现权限档位与临时工作区 |
| 2026-09-02 | P10 | 完成 | 主系统提示词改为中文任务执行契约：当前目标判断、分析只读、检查/修改/验证闭环、必要注释、工具与沙箱边界、基于证据的最终回复；内部上下文摘要提示词同步中文化 | `PYTHONPATH=src python -m unittest discover -s tests -t .` 全量通过；覆盖主提示词内容、运行时传递与摘要 JSON 字段契约 | 以真实任务观察模型的中文执行与最终汇报质量 |
| 2026-09-02 | P11 | 完成 | 移除会话总轮次硬上限及其配置与界面计数；旧 SQLite 字段保持惰性兼容，旧 `limit_reached` 会话恢复为可继续 | `PYTHONPATH=src python -m unittest discover -s tests -t .`：73 项通过；`npm run build` 通过 | 重启服务后发送多条消息确认持续会话 |

## 更新模板

```text
日期：YYYY-MM-DD
阶段：P0 / P1 / P2 / P3 / P4
状态：进行中 / 完成 / 受阻
完成内容：
验证方式：
遗留风险或阻塞：
下一步：
```
